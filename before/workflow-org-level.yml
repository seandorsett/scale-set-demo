# THIS FILE IS FOR PRESENTATION REFERENCE ONLY
# It is NOT in .github/workflows/ — it cannot be triggered
# Walk through this file on screen to show what legacy ARC workflows looked like

name: "BEFORE: Legacy ARC Org Workflow"

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: [self-hosted, linux, x64, legacy-arc, repo-demo]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show shared runner context
        run: |
          echo "Legacy ARC shared runner"
          echo "Hostname: $(hostname)"
          echo "Kernel: $(uname -sr)"
          echo "Repository: $GITHUB_REPOSITORY"

      - name: Run organization policy checks
        run: |
          echo "Applying shared organization compliance checks"
          echo "Using runner group access plus legacy ARC labels"

      - name: Run build and test
        run: |
          echo "Running standardized CI pipeline"
          echo "Packaging artifact for downstream deployment"

      - name: Publish legacy ARC notes
        run: |
          echo "This workflow assumed org access via runner groups and ARC labels."
          echo "Capacity came from a long-lived shared runner pool, not per-job pods."
          echo "Scaling and warm capacity were tuned through HorizontalRunnerAutoscaler."
